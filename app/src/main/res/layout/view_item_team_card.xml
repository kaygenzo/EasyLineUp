<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/itemTeamCardContainer"
    tools:background="@color/team_type_bottom_view">

    <ImageView
        android:id="@+id/itemTeamCardIcon"
        android:layout_width="30dp"
        android:layout_height="30dp"
        app:srcCompat="@drawable/ic_list_black_24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:tint="@color/white"/>

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/itemTeamCardDescription"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toRightOf="@+id/itemTeamCardIcon"
        app:layout_constraintRight_toRightOf="parent"
        tools:text="Your team participated to 32 tournaments and 98 games"
        android:layout_marginStart="@dimen/default_margin"
        android:textColor="@color/white"/>


</androidx.constraintlayout.widget.ConstraintLayout>